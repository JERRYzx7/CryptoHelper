app = 'crypto-scanner'
primary_region = 'sin'  # Singapore - Binance friendly

[build]

[env]
  STATE_DIR = '/data'

[mounts]
  source = 'scanner_data'
  destination = '/data'
  initial_size = '1gb'

[[vm]]
  size = 'shared-cpu-1x'
  memory = '256mb'
  cpus = 1
